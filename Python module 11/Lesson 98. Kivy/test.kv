BoxLayout:
    BoxLayout:
        id: box_test
        Button: 
            text: 'Hello, kivy.'
            on_press: app.command(self)
    BoxLayout:
        orientation: 'vertical'
        # this layout is populated with Button widgets in app code
        id: entries_box

